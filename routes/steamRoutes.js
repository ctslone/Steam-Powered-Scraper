var axios = require("axios");
var cheerio = require("cheerio");

// will need a route for new, top, current and upcoming games
// need a route for saved articles
    // when user clicks the save button, it uses some verson of this.(id?) and takes the ID saved on the card and button and then adds that particular game to the saved table
// need a table for each type of game
// need a table for saved games

module.exports = function(app) {

}